# Copy this file to .env and fill values before running

# Postgres connection (used by task3 and task4)
DB_HOST=localhost
DB_PORT=5324
DB_NAME=classicmodels
DB_USER=postgres
DB_PASSWORD=postgres

# Groq API credentials (replace with your own key)
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

