A SQL query failed during execution. Fix it.

Original question: {question}

Failed SQL:
{sql}

PostgreSQL error:
{error}

{schema}

Common mistakes to check:
- Column names not double-quoted (e.g. customerNumber should be "customerNumber")
- Wrong column name for the table (check schema carefully)
- Missing or wrong JOIN condition
- Selecting columns from wrong table
- GROUP BY missing when using aggregates with other columns

Respond ONLY with a valid JSON object:
{{
  "sql": "the corrected SELECT SQL query",
  "fix_explanation": "concise description of what was wrong and what you changed"
}}
